{{#equals export 'reference'}}
{{#if deepRelations.length}}Omit<
{{~/if~}}SetRequired<{{>base}},
{{#each relations}}"{{{this}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{#if deepRelations.length}}
, {{#each deepRelations}}"{{{name}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{~/if~}}
{{#if deepRelations.length}}
& {
  {{#each deepRelations}}
    {{{name}}}: {{#if isArray~}}Array<{{/if}}SetRequired<{{base}}, {{#each properties}}"{{{this}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{#if isArray~}}>{{/if}}
  {{/each}}
}
{{~/if~}}
{{>isNullable}}
{{~else equals export 'array'}}
Array<{{#if deepRelations.length}}Omit<
{{~/if~}}SetRequired<{{>base}},
{{#each relations}}"{{{this}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{#if deepRelations.length}}
, {{#each deepRelations}}"{{{name}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{~/if~}}
{{#if deepRelations.length}}
& {
  {{#each deepRelations}}
    {{{name}}}: {{#if isArray~}}Array<{{/if}}SetRequired<{{base}}, {{#each properties}}"{{{this}}}"{{#unless @last}} | {{/unless}}{{/each}}>{{#if isArray~}}>{{/if}}
  {{/each}}
}
{{~/if~}}
>{{>isNullable}}
{{~/equals}}